server {
  server_name ecommerce-shop-project.com;

  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }

  location ~ /v1 {
    proxy_pass http://localhost:5000;
  }
}
